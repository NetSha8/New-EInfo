---
---

<svg
  class="absolute inset-0 w-full h-full pointer-events-none"
  viewBox="0 0 200 200"
  preserveAspectRatio="xMidYMid meet"
  xmlns="http://www.w3.org/2000/svg"
  aria-hidden="true"
>
  <g fill="none" stroke="oklch(0.58 0.190 250)">
    <circle class="br" style="--d:0s"   cx="100" cy="100" r="22" stroke-width="0.9"/>
    <circle class="br" style="--d:0.7s" cx="100" cy="100" r="40" stroke-width="0.7"/>
    <circle class="br" style="--d:1.4s" cx="100" cy="100" r="60" stroke-width="0.5"/>
    <circle class="br" style="--d:2.1s" cx="100" cy="100" r="82" stroke-width="0.35"/>
  </g>
</svg>

<style>
  .br {
    transform-box: fill-box;
    transform-origin: center;
    opacity: 0;
  }

  @media (prefers-reduced-motion: no-preference) {
    .br {
      animation: broadcast 3.2s var(--d, 0s) cubic-bezier(0.19, 1, 0.22, 1) infinite;
    }

    @keyframes broadcast {
      0%   { transform: scale(0.65); opacity: 0.55; }
      100% { transform: scale(1.5);  opacity: 0;    }
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .br { opacity: 0.12; }
  }
</style>
